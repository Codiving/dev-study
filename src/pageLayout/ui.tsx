import styled from "@emotion/styled";

export const FlexDescription = styled("p")(() => ({
  padding: "5px 0",
  fontSize: 14
}));

export const FlexTypography = styled("p")(() => ({
  padding: "5px 0"
}));

export const FlexTitle = styled("h3")(() => ({
  fontSize: 24,
  fontWeight: "bold"
}));
